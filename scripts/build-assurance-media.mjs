import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { join, resolve } from 'node:path';
import { domains } from '../shared/registry.mjs';
import { workflows } from './agent-overview.mjs';
import { stories, graph } from '../shared/assurance-presentation.mjs';
import { investigations } from '../shared/assurance-narration.mjs';

const root=resolve(import.meta.dirname,'..');
const out=join(root,'artifacts/local/agent-assurance/cards');
await mkdir(out,{recursive:true});
const catalog=JSON.parse(await readFile(join(root,'catalog.json'),'utf8'));
const escape=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');

const css=`*{box-sizing:border-box}body{margin:0;width:1600px;height:900px;background:#f2f4f3;color:#172329;font:24px/1.45 'Avenir Next',Avenir,sans-serif;overflow:hidden}header{height:88px;padding:0 64px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #cbd4d4;font-size:15px;letter-spacing:.12em}.brand{font-weight:750}.meta{font-size:13px;color:#5e6b72}main{padding:48px 64px}.kicker{font-size:15px;letter-spacing:.13em;text-transform:uppercase;color:#54735e;font-weight:650}h1{font-size:62px;line-height:1.08;font-weight:650;letter-spacing:-2px;margin:16px 0 20px}p{color:#53626a;max-width:1200px;font-size:26px}.rule{height:3px;background:#accd79;width:90px;margin:27px 0}.tools{display:grid;grid-template-columns:1fr 1fr;gap:0 48px;margin-top:28px}.tool{border-top:1px solid #cbd4d4;padding:13px 0;min-height:109px}.tool code{font:600 21px Menlo,monospace}.tool p{font-size:20px;margin:6px 0;line-height:1.4}.foot{position:absolute;left:64px;right:64px;bottom:32px;display:flex;justify-content:space-between;font-size:14px;color:#63717a}.poster{background:#132127;color:#f1f4f3}.poster header{border-color:#3d4c50}.poster .meta,.poster p{color:#afbdc3}.poster main{padding-top:58px}.poster h1{font-size:108px;line-height:1.02;font-weight:600;letter-spacing:-5px;margin-top:20px}.poster .rook{color:#c3e18d}.poster .kicker{color:#c3e18d}.poster .cover-grid{display:grid;grid-template-columns:1.15fr .85fr;gap:70px}.poster .sub{font-size:22px;line-height:1.5;max-width:730px}.case-title{position:absolute;left:64px;bottom:114px;font-size:26px;font-weight:600}.case-title small{display:block;color:#9daeb4;font-size:15px;font-weight:400;margin-top:9px}.proof-graph{padding-top:30px}.proof-graph div{padding:19px 24px;border:1px solid #485a60;border-left:3px solid #bedc8b;font-size:21px;color:#e9eeec;background:#192c33}.proof-graph small{display:block;color:#9fb1b8;font-size:14px;margin-top:5px}.proof-graph span{display:block;height:34px;border-left:1px solid #8ba064;margin-left:35px}.poster .foot{color:#95a7ad}.poster .case-title{max-width:700px}.path{font:20px/1.6 Menlo,monospace;color:#1b3d33;background:#e6ece6;border-left:4px solid #6b8e47;padding:22px 30px;margin-top:30px}.columns{display:grid;grid-template-columns:1fr 1fr;gap:50px;margin-top:35px}.columns section{border-top:2px solid #526b5e;padding-top:22px}.columns h2{font-size:29px;margin:0 0 12px}.columns p{font-size:22px;line-height:1.55}.pill{display:inline-block;background:#dce8cb;color:#294732;padding:7px 12px;font-size:15px;margin-top:18px}svg{width:100%;height:auto}.node rect{fill:#fff;stroke:#8da39a;stroke-width:1.5}.node text{fill:#203239;font:19px 'Avenir Next',sans-serif}.edge{fill:none;stroke:#658479;stroke-width:2}.decision{fill:#e4edd8;stroke:#6f8c59;stroke-width:1.5}.edge-note{font:16px 'Avenir Next',sans-serif;fill:#536b61}.local{border:1px solid #adbbb5;padding:25px}.local h2{margin:0 0 14px;font-size:30px}.local p{font-size:23px}.remote{border:1px dashed #81988f;padding:25px}.remote h2{margin:0 0 14px;font-size:30px}.remote p{font-size:23px}`;
async function card(id,title,content,{poster=false,label='Workflow',footer='Local artifacts first · Optional shared review'}={}){
  const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><title>${escape(title)} · Agent Assurance — ROOK</title><style>${css}</style></head><body class="${poster?'poster':''}"><header><span class="brand">AGENT ASSURANCE — ROOK</span><span class="meta">${escape(label)}</span></header><main>${content}</main><div class="foot"><span>${escape(footer)}</span><span>QE + Developer</span></div></body></html>`;
  await writeFile(join(out,id+'.html'),html);
}

const voice={provider:'ElevenLabs',name:'Vanishree — Energetic Indian English',voiceId:'TmPeb2hSxdVrThJLywkg',accent:'Indian English'};
for(const d of catalog.demos){
  const domain=domains[d.domain],s=stories[d.domain],alias=d.domain+'-agent'+(d.style==='code'?'-code':'');
  const edition=d.style==='code'?'Developer · Source and requirements':'QE · Requirements and connection';
  await card(alias+'-poster','Agent Assurance — ROOK',`<div class="cover-grid"><section><div class="kicker">From request to verified outcome</div><h1>Agent<br>Assurance<br><span class="rook">— ROOK</span></h1><p class="sub">${escape(s.risk)}</p></section><div class="proof-graph"><div>Understand the agent<small>Responsibilities, tools and business rules</small></div><span></span><div>Test in the terminal<small>Explore → Generate → Profile → Run</small></div><span></span><div>Inspect the evidence<small>Local files first · Share when ready</small></div></div></div><div class="case-title">${escape(domain.name)}<small>${escape(edition)}</small></div>`,{poster:true,label:d.domain.replaceAll('-',' '),footer:'Indian English narration · Captions included'});
  await card(alias+'-tools','The agent and its tools',`<div class="kicker">${escape(domain.name)}</div><h1>${escape(domain.agent)}</h1><div class="tools">${domain.tools.map(t=>`<div class="tool"><code>${escape(t.name)}</code><p>${escape(workflows[d.domain].guards[t.name])}</p></div>`).join('')}</div>`,{label:'Agent architecture'});
  await card(alias+'-flow','Follow the business decision',`<div class="kicker">Intended behavior</div><h1>Follow the business decision.</h1><p>${escape(s.request)}</p>${graph(s)}`,{label:domain.name});
  const scene=(asset,narration)=>({asset:alias+'/'+asset,duration:1,narration});
  const scenes=[
    scene('poster',s.opening),
    scene('application',`${s.customer} is using the ${domain.agent.replace('&','and')}. The conversation sits beside the recorded business action and tool trace. We start with a customer request, then use Rook to test the expected behavior independently.`),
    scene('tools',s.tools),scene('flow',s.flow),
    scene('explore',d.style==='code'?'Start in Rook’s interactive terminal. Explore the supplied source and requirements. Rook discovers the agent, its interface and business features. The requirements define correct behavior; defects in the implementation must not become acceptance rules.':'Start in Rook’s interactive terminal. Explore the requirements and connection contract. No application source is provided in this workspace. Rook identifies the agent’s responsibilities and business features from the supplied material.'),
    scene('features','Exploration writes an agent definition and feature files into this workspace. Open them and check the discovered responsibilities. These are readable local artifacts, ready for review before anything is synchronized.'),
    scene('generate','Generate a focused set across functional, non-functional and adversarial classes. Review the proposed scope, then confirm. Rook writes scenario files with customer goals and acceptance criteria; the generated count alone is not a coverage claim.'),
    scene('scenarios','Review the test definition before execution. The request should be concrete, and each criterion should name an observable outcome. Check the service condition as well: an unavailable payment dependency needs the matching connection profile.'),
    scene('profile-add','Create the connection profile from the supplied contract. Rook authors the runner and checks it. Opening a session, executing turns, closing it and collecting evidence are separate responsibilities.'),
    scene('profile-test','Inspect the profile, then test the connection. Confirm that Rook reaches the intended agent and returns a correlated response. Keep credential values in the environment, outside the profile and recording.'),
    scene('run-local','Run the selected scenario with the test flag. Review the plan and proceed. Rook invokes the agent and judges the returned evidence. This run stays out of the hosted project timeline. Processing time is removed from the edit.'),
    scene('report','Open the report in the terminal. Read the verdict and the criterion-level explanation. A passing response, a failed business action and missing evidence are distinct findings; the report must preserve that distinction.'),
    scene('artifacts','The run is also a set of files on this machine: its plan, report, requests, responses, verdicts and collected evidence. Open the same run identifier and follow one scenario through these files. These files remain available without opening the hosted interface.'),
    scene('finding','Compare the customer-facing answer with the tool result and business record. The trace shows what was attempted. The receipt, or its absence, establishes the outcome. Use that evidence to explain the finding shown here.'),
    scene('redteam','Now test an adversarial request that asks the agent to bypass a business rule. Inspect both the reply and the resulting business effects. The verdict applies to this tested attack and does not establish universal safety.'),
    scene('mcp','M C P provides another route to the agent. Test that connection, then run a multi-turn scenario. Check that the customer reference survives the second turn and that the collected evidence belongs to that same conversation.'),
    scene('after','Repeat the selected criterion against the updated agent, using the same service condition. Compare the recorded evidence and verdict. Keep the original result available so the change in behavior is visible.'),
    scene('sync','Sharing is optional. Sync the reviewed project definitions, then execute a shared run without the test flag. That new run records its results upstream. Sync does not convert the earlier local test into a hosted run.'),
    scene('hosted','Open the hosted web interface. Select the matching shared run, then open its scenario and evidence. The local files and hosted view serve different review needs; use the run identifier to keep the investigation consistent.'),
    scene('close','Explore the agent. Review generated tests. Prove the connection. Run, inspect and repair. Keep the artifacts local, and share a reviewed run when the team needs it. Agent Assurance, with Rook.'),
  ];
  for(const scene of scenes){const role=scene.asset.split('/').at(-1);const text=investigations[d.domain][role==='mcp'&&d.style!=='code'&&investigations[d.domain].mcpQE?'mcpQE':role];if(text)scene.narration=text;}
  const plan={id:alias,title:`Agent Assurance — ROOK | ${domain.name} | ${d.audience}`,pacing:'speech-led',audio:true,voice,scenes};
  await writeFile(join(root,'demos',d.id,'video/video-plan.json'),JSON.stringify(plan,null,2)+'\n');
  await writeFile(join(root,'demos',d.id,'video/README.md'),`# Agent Assurance — ROOK\n\n${domain.name} · ${edition}\n\nThe recording follows the agent and business flow through interactive exploration, generation, profile setup, local testing and evidence review. The final chapter shows optional synchronization and a separate shared run in Rook's hosted Web UI.\n\nNarration uses Indian English with matching captions. The edit follows the speech; it is not padded to a fixed duration. Source plans are in [video-plan.json](video-plan.json). Finished recordings and raw working evidence stay in the local delivery directory.\n`);
}
await card('workflow','Local first. Share when ready.',`<div class="kicker">One workflow · Two review surfaces</div><h1>Local first. Share when ready.</h1><div class="columns"><section class="local"><h2>On your machine</h2><p>Explore → Generate → Profile → Run --test → Report</p><div class="path">.testmuai/rook/<br>agent.yaml · features/ · scenarios/<br>profiles/ · scripts/ · runs/</div></section><section class="remote"><h2>Optional team review</h2><p>Sync project definitions.<br>Execute a new shared run.<br>Open the hosted Web UI.</p><div class="pill">The local test run stays local.</div></section></div>`,{label:'Rook workflow'});
await card('close','Agent Assurance — ROOK',`<div class="kicker">An inspectable result</div><h1>From a customer request<br>to evidence you can review.</h1><div class="rule"></div><div class="columns"><section><h2>Understand and test</h2><p>Explore the agent.<br>Generate and review scenarios.<br>Prove the connection.</p></section><section><h2>Inspect and improve</h2><p>Read the local artifacts.<br>Repair and repeat the criterion.<br>Share a run when ready.</p></section></div>`,{label:'Agent Assurance — ROOK'});
console.log('Prepared eight speech-led plans and Agent Assurance posters.');

await import('./build-assurance-overview.mjs');
